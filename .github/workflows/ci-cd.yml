name: CI/CD Pipeline

on:
  push:
    branches: [ dev, staging, master ]
  pull_request:
    branches: [ dev, staging, master ]

env:
  EC2_HOST: ec2-18-117-195-190.us-east-2.compute.amazonaws.com
  EC2_IP: 18.117.195.190
  EC2_USER: admin
  DOCKER_USERNAME: ppsfafire

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'

    - name: Install backend dependencies
      run: |
        cd backend
        npm ci

    - name: Install frontend dependencies
      run: |
        cd frontend
        npm ci

    - name: Run backend tests
      run: |
        cd backend
        npm test --if-present

    - name: Run frontend tests
      run: |
        cd frontend
        npm test --if-present

  build-and-push:
    needs: test
    runs-on: ubuntu-latest
    if: github.event_name == 'push'
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build and push backend image
      run: |
        cd backend
        docker build -t ${{ env.DOCKER_USERNAME }}/devops-backend:${{ github.sha }} .
        docker build -t ${{ env.DOCKER_USERNAME }}/devops-backend:latest .
        docker push ${{ env.DOCKER_USERNAME }}/devops-backend:${{ github.sha }}
        docker push ${{ env.DOCKER_USERNAME }}/devops-backend:latest

    - name: Build and push frontend image
      run: |
        cd frontend
        docker build --build-arg REACT_APP_API_URL=http://${{ env.EC2_IP }}:5000 -t ${{ env.DOCKER_USERNAME }}/devops-frontend:${{ github.sha }} .
        docker build --build-arg REACT_APP_API_URL=http://${{ env.EC2_IP }}:5000 -t ${{ env.DOCKER_USERNAME }}/devops-frontend:latest .
        docker push ${{ env.DOCKER_USERNAME }}/devops-frontend:${{ github.sha }}
        docker push ${{ env.DOCKER_USERNAME }}/devops-frontend:latest

  deploy-dev:
    needs: build-and-push
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/dev'
    environment: development
    
    steps:
    - name: Deploy to EC2
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ env.EC2_IP }}
        username: ${{ env.EC2_USER }}
        key: ${{ secrets.EC2_SSH_KEY }}
        script: |
          # Criar diretório se não existir
          sudo mkdir -p /opt/devops-app
          sudo chown $USER:$USER /opt/devops-app
          cd /opt/devops-app
          
          # Adicionar usuário ao grupo docker se não estiver
          if ! groups $USER | grep -q docker; then
            echo "Adicionando usuário ao grupo docker..."
            sudo usermod -aG docker $USER
            # Recarregar grupos (pode não funcionar em SSH, mas tentamos)
            newgrp docker || true
          fi
          
          # Instalar docker-compose se não estiver instalado
          if ! command -v docker-compose &> /dev/null; then
            echo "Instalando docker-compose..."
            sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
            sudo chmod +x /usr/local/bin/docker-compose
          fi
          
          # Criar docker-compose.yml se não existir
          if [ ! -f docker-compose.yml ]; then
            cat > docker-compose.yml << 'EOF'
          version: '3.8'
          
          services:
            frontend:
              image: ppsfafire/devops-frontend:latest
              ports:
                - "3000:3000"
              environment:
                - REACT_APP_API_URL=http://localhost:5000
              depends_on:
                - backend
              restart: unless-stopped
            
            backend:
              image: ppsfafire/devops-backend:latest
              ports:
                - "5000:5000"
              environment:
                - NODE_ENV=production
                - PORT=5000
              restart: unless-stopped
              volumes:
                - ./logs:/app/logs
          
          networks:
            default:
              driver: bridge
          EOF
          fi
          
          # Parar containers existentes
          sudo docker-compose down || true
          
          # Fazer pull das imagens
          sudo docker-compose pull
          
          # Iniciar containers
          sudo docker-compose up -d
          
          # Verificar status
          sudo docker-compose ps
          
          # Aguardar um pouco e verificar logs
          sleep 10
          echo "=== Logs do Backend ==="
          sudo docker-compose logs backend --tail=20
          echo "=== Logs do Frontend ==="
          sudo docker-compose logs frontend --tail=20

    - name: Health check
      run: |
        echo "🔍 Iniciando health check..."
        sleep 30
        echo "📡 Testando conectividade com EC2..."
        
        # Testar se o EC2 responde via HTTP (mais confiável que ping)
        echo "📡 Testando backend na porta 5000..."
        
        # Testar backend com timeout
        if curl -f -m 10 http://${{ env.EC2_IP }}:5000/api/health; then
          echo "✅ Backend respondeu com sucesso"
        else
          echo "❌ Backend não respondeu (timeout 10s)"
        fi
        
        echo "📡 Testando frontend na porta 3000..."
        
        # Testar frontend com timeout
        if curl -f -m 10 http://${{ env.EC2_IP }}:3000; then
          echo "✅ Frontend respondeu com sucesso"
        else
          echo "❌ Frontend não respondeu (timeout 10s)"
        fi
        
        echo "✅ Health check concluído!"

  deploy-staging:
    needs: build-and-push
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/staging'
    environment: staging
    
    steps:
    - name: Deploy to EC2 (Staging)
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ env.EC2_IP }}
        username: ${{ env.EC2_USER }}
        key: ${{ secrets.EC2_SSH_KEY }}
        script: |
          # Criar diretório se não existir
          sudo mkdir -p /opt/devops-app
          sudo chown $USER:$USER /opt/devops-app
          cd /opt/devops-app
          
          # Adicionar usuário ao grupo docker se não estiver
          if ! groups $USER | grep -q docker; then
            echo "Adicionando usuário ao grupo docker..."
            sudo usermod -aG docker $USER
            # Recarregar grupos (pode não funcionar em SSH, mas tentamos)
            newgrp docker || true
          fi
          
          # Instalar docker-compose se não estiver instalado
          if ! command -v docker-compose &> /dev/null; then
            echo "Instalando docker-compose..."
            sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
            sudo chmod +x /usr/local/bin/docker-compose
          fi
          
          # Criar docker-compose.yml se não existir
          if [ ! -f docker-compose.yml ]; then
            cat > docker-compose.yml << 'EOF'
          version: '3.8'
          
          services:
            frontend:
              image: ppsfafire/devops-frontend:latest
              ports:
                - "3000:3000"
              environment:
                - REACT_APP_API_URL=http://localhost:5000
              depends_on:
                - backend
              restart: unless-stopped
            
            backend:
              image: ppsfafire/devops-backend:latest
              ports:
                - "5000:5000"
              environment:
                - NODE_ENV=production
                - PORT=5000
              restart: unless-stopped
              volumes:
                - ./logs:/app/logs
          
          networks:
            default:
              driver: bridge
          EOF
          fi
          
          # Parar containers existentes
          sudo docker-compose down || true
          
          # Fazer pull das imagens
          sudo docker-compose pull
          
          # Iniciar containers
          sudo docker-compose up -d
          
          # Verificar status
          sudo docker-compose ps
          
          # Aguardar um pouco e verificar logs
          sleep 10
          echo "=== Logs do Backend ==="
          sudo docker-compose logs backend --tail=20
          echo "=== Logs do Frontend ==="
          sudo docker-compose logs frontend --tail=20

    - name: Health check (Staging)
      run: |
        echo "🔍 Iniciando health check (Staging)..."
        sleep 30
        echo "📡 Testando conectividade com EC2..."
        
        # Testar se o EC2 responde via HTTP (mais confiável que ping)
        echo "📡 Testando backend na porta 5000..."
        
        # Testar backend com timeout
        if curl -f -m 10 http://${{ env.EC2_IP }}:5000/api/health; then
          echo "✅ Backend respondeu com sucesso"
        else
          echo "❌ Backend não respondeu (timeout 10s)"
        fi
        
        echo "📡 Testando frontend na porta 3000..."
        
        # Testar frontend com timeout
        if curl -f -m 10 http://${{ env.EC2_IP }}:3000; then
          echo "✅ Frontend respondeu com sucesso"
        else
          echo "❌ Frontend não respondeu (timeout 10s)"
        fi
        
        echo "✅ Health check concluído!"

  deploy-production:
    needs: build-and-push
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/master'
    environment: production
    
    steps:
    - name: Deploy to EC2 (Production)
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ env.EC2_IP }}
        username: ${{ env.EC2_USER }}
        key: ${{ secrets.EC2_SSH_KEY }}
        script: |
          # Criar diretório se não existir
          sudo mkdir -p /opt/devops-app
          sudo chown $USER:$USER /opt/devops-app
          cd /opt/devops-app
          
          # Adicionar usuário ao grupo docker se não estiver
          if ! groups $USER | grep -q docker; then
            echo "Adicionando usuário ao grupo docker..."
            sudo usermod -aG docker $USER
            # Recarregar grupos (pode não funcionar em SSH, mas tentamos)
            newgrp docker || true
          fi
          
          # Instalar docker-compose se não estiver instalado
          if ! command -v docker-compose &> /dev/null; then
            echo "Instalando docker-compose..."
            sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
            sudo chmod +x /usr/local/bin/docker-compose
          fi
          
          # Criar docker-compose.yml se não existir
          if [ ! -f docker-compose.yml ]; then
            cat > docker-compose.yml << 'EOF'
          version: '3.8'
          
          services:
            frontend:
              image: ppsfafire/devops-frontend:latest
              ports:
                - "3000:3000"
              environment:
                - REACT_APP_API_URL=http://localhost:5000
              depends_on:
                - backend
              restart: unless-stopped
            
            backend:
              image: ppsfafire/devops-backend:latest
              ports:
                - "5000:5000"
              environment:
                - NODE_ENV=production
                - PORT=5000
              restart: unless-stopped
              volumes:
                - ./logs:/app/logs
          
          networks:
            default:
              driver: bridge
          EOF
          fi
          
          # Parar containers existentes
          sudo docker-compose down || true
          
          # Fazer pull das imagens
          sudo docker-compose pull
          
          # Iniciar containers
          sudo docker-compose up -d
          
          # Verificar status
          sudo docker-compose ps
          
          # Aguardar um pouco e verificar logs
          sleep 10
          echo "=== Logs do Backend ==="
          sudo docker-compose logs backend --tail=20
          echo "=== Logs do Frontend ==="
          sudo docker-compose logs frontend --tail=20

    - name: Health check (Production)
      run: |
        echo "🔍 Iniciando health check (Production)..."
        sleep 30
        echo "📡 Testando conectividade com EC2..."
        
        # Testar se o EC2 responde via HTTP (mais confiável que ping)
        echo "📡 Testando backend na porta 5000..."
        
        # Testar backend com timeout
        if curl -f -m 10 http://${{ env.EC2_IP }}:5000/api/health; then
          echo "✅ Backend respondeu com sucesso"
        else
          echo "❌ Backend não respondeu (timeout 10s)"
        fi
        
        echo "📡 Testando frontend na porta 3000..."
        
        # Testar frontend com timeout
        if curl -f -m 10 http://${{ env.EC2_IP }}:3000; then
          echo "✅ Frontend respondeu com sucesso"
        else
          echo "❌ Frontend não respondeu (timeout 10s)"
        fi
        
        echo "✅ Health check concluído!"

    - name: Notify deployment success
      run: |
        echo "🚀 Deploy para produção concluído com sucesso!"
        echo "📅 Data: $(date)"
        echo "🔗 Branch: ${{ github.ref }}"
        echo "👤 Autor: ${{ github.actor }}"
        echo "🌐 URL: http://${{ env.EC2_IP }}"
